export
let vertices = Float32Array.from([
    -0.5, 0, 0,
    0.5, 0, 0,
    0, 1, 0
]);

export
let indices = Uint16Array.from([0, 1, 2]);

export
let normals = Float32Array.from([
    0, 0, 1, 0, 0, 1, 0, 0, 1
]);
